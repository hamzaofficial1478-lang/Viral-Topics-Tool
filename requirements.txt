# ShortForge Phase 1 runtime dependencies.
# System requirements (install separately): ffmpeg + ffprobe.
#   Debian/Ubuntu:  sudo apt-get install -y ffmpeg
#   macOS:          brew install ffmpeg
yt-dlp>=2024.1.0
faster-whisper>=1.0.0
PyYAML>=6.0

# Optional — enables Claude-backed hook detection instead of the keyword
# heuristic. Set ANTHROPIC_API_KEY to use it. Uncomment to install:
# anthropic>=0.40.0
